import { FiltroLavadoLozaPipe } from './filtro-lavado-loza.pipe';

describe('FiltroLavadoLozaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroLavadoLozaPipe();
    expect(pipe).toBeTruthy();
  });
});
