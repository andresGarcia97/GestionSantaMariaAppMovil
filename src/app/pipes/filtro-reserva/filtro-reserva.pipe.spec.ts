import { FiltroReservaPipe } from './filtro-reserva.pipe';

describe('FiltroReservaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroReservaPipe();
    expect(pipe).toBeTruthy();
  });
});
